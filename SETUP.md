# Publish this GitHub profile README

Your active public GitHub username is `Propaganda89`, so GitHub will display this README on your profile when it lives in a public repository with that exact name.

1. Create a new **public** repository named `Propaganda89`.
2. Upload `README.md` and the complete `assets` folder from this package.
3. Keep the repository public and visit `https://github.com/Propaganda89`.
4. In your GitHub profile settings, enable private contribution visibility if you want private activity reflected in your contribution graph.

The custom header uses a relative path (`./assets/profile-header.svg`), so keep the folder structure unchanged.

